{
  "name": "cleanup-after",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Michael Medin <michael@medin.name>",
  "license": "MIT",
  "scripts": {
    "build": "mkdir lib && cp cleanup.js lib/ && cp index.js lib/"
  },
  "dependencies": {
    "@actions/core": "1.2.3",
    "@actions/io": "^1.0.2"
  }
}
